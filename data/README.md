# Data directory

Put read-only dashboard source files here.

Suggested files:

- `KANBAN.md`
- `daily-brief.html`

If a file is missing, the feed service will show a placeholder message instead of failing.
